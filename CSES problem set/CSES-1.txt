CSES->

Introductory Problem:


1)weird algo->

while(n!=1)
{
 System.out.println(n);
	if(n%2==0)
	{
		n/=2;
	}
	else
	{
		n=3n+1;
	}

}
System.out.println(1);
}

2)
 missing number

	expected=n(n+1)/2;
	actualSum+=nums[i];
	return expected-actual

3)
	int current=1;
	int max=Integer.MIN_VALUE;	for(int i=1;i<s.length();i++)
	{
		if(s.charAt(I)==s.charAt(i-1){
current++}
else
{
current=1}
max=Math.max(max,current);


4) repeated number
forward loop means
   if(nums[i+1]<nums[i])
	{
	cost++;
	nums[i+1]=nums[i];	}
return cost?

wait 
if(nums[i]>nums[i+1]) {
    cost +=nums[i] - nums[i+1];
    nums[i+1]=nums[i];
}

5)permutations so basically we shouldn't have Math.abs(nums[i]-nums[i+1])!=1 and the intended pattern here is to print all even numbers till n and then print odd numbers

6) number spiral ->max(y,x) will be considered as n if n is even-> if(y==n) ans=n*n-y+1 		else 			ans=(n-1)*(n-1)+x
else
if(x==n) then ans=n*n-y+1 else ans=(n-1)*(n-1)+x

7) two knights problem -> possible requirement of nCr formula?  nope it is total = (k*k)*(k*k - 1)/2 which is nC2 

attacking = 4*(k-1)*(k-2) 4 because  2*3 rectangle 2 times and 3*2 rectangle again 2 times attacking pairs 

answer = total - attacking 

8) two set-> we already have the partition subset equal sum follow same then 
largest possible number eventually we will reach 0 exactly
for (int i = n; i >= 1; i--) {
    if (i <= target) {
        set1.add(i);
        target -= i;
    } else {
        set2.add(i);
    }
}

n=8-> 8+7+6+5+4+3+2+1-> 36
target=18

we know 36%2==0 so yes possible to divide ->18 target so take from 8 rem-> 10 then if I take 7 then rem-> 3 then skip 6,5,4 then take 3 rem=0 skip 1 and 2
then set1 will have 8,7, 3 set2 we have 6,5,4,2,1 [reverse loop running]


9) bit string-> basically 2 power n is solution->while(b>0)
{
 if((b&1)==1) then result=result*a%mod
a=a*%mod b>>=1;
}
return result

9)trailing zeroes -> count factors of 5 to n long ans=0; while(n>0){n/=5 ans+=n;}


10) coin piles
 we know the problem related to 3 somehow so try condition 1 is a+b%3==0 then since some of them will fail then if one pile contains more than twice the coins of the other, the smaller pile will become empty before the larger pile can to zero. Hence max(a,b) must not exceed 2 * min(a,b) 
Math.max(a,b) <= 2 * Math.min(a,b) solves for large one side and small other side
if ((a + b)%3 ==0&&Math.max(a,b) <= 2 *Math.min(a,b))
{
   print YES;
}
else
{
   print NO;
}

11) gray code generator prev state is important to cconstruct for next state prevstate add 0 at front then prevstate reverse add 1 at front combine is number we need -> for(int i=0;i<(1<<n);i++) gray=i^(i<<1)
<< means left shift *2 it means i<2 power n


12)Tower of Hanoi

hanoi(n, source, auxiliary, destination)

1)hanoi(n-1, source, destination, auxiliary)
2)move source -> destination
3)hanoi(n-1, auxiliary, source, destination)

T(n)=2T(n-1)+1
so T(n-1) is just again recursive to get 2 power n -> 2^n-1

13) Creating string Becomes same permutation problem classic use bactrack+recusion and then create string from list u stored


14)Apple distribution good thinking subset problem again with backtrack

solve(0,0,nums) 
total+=nums[i]
if(i==nums.length)-> diff=Math.abs(total-2*sum) ans=math.min(ans,diff) return;
solve(i+1,sum+nums[i],nums) 
solve(i+1,sum,nums[i])

total-2*sum because group1-> sum group2=total-sum group1-group2= 2*sum-total math.abs use it 

15)digit queries->
divide into blocks 1 digit, 2 digit block 3 digit block 
long digit=1;
long count=9;
long start=1;
while(k>digit*count)
{
k-=digit*count;
digit++;
count*=10
start*=10;
}

long range=(start)+(k-1)/digits;
long index=(k-1)%digits
String.valueOf(number).charAt(index)

for k=250

61 means from 100 start next 61 100+(60)/3 120 then we need index-> 60%3-> 0 so 1


16)Rabb Game similar to course schedule if ordering needed course schedule 2=>

build graph using edges then graph[u].add(v)

then for(int i=0;i<numCourses;i++)
{

if(hasCycle(graph,i,states)){ return false} return true 

then if(state[u]==1) return true if(state[u]==2) return false state[u]=1 we ste initially then for(int nei:graph[u])
{
if(hasCycle(graph,nei,states))
{
	return true
}

state[u]=2;
return false
 
17) mex grid construction first I thought the pattern is similar to next permutation and rotate array but in 2d but simple xor is needed to be done

for(int i=0;i<n;i++){
    for(int j=0;j<n;j++){
        System.out.print((i ^ j) + " ");
    }
    System.out.println();
}


18)knight moves grid-> DFS + Backtracking and warnsdoff rule to choose next square with max degree

the code is big so here is pseudocode

dfs(r,c,step){

    board[r][c] = step;

    if(step == 64)
        return true;
    List<Move> moves = validMoves(r,c);
    sort by degree ascending;
    for(move : moves){
        if(dfs(nextR,nextC,step+1))
            return true;
    }
    board[r][c] = 0;
    return false;
}

possible moves for knight always 8 fixed chess moves direction array create with that then newRow and newCol


19) grid coloring 1:

if adjacent same colors then false right A B and C D -> if would be better for(int i=0;i<n;i++){for(int j=0;j<n;j++){if(i%2==0){ans[i][j]=(j%2==0)?a:b
}else
{
ans[i][j]=(j%2==0)?c:d
}
if(ans[i][j]==grid[i][j]) return false convert to string [ansi][j]]return true


20) chess board and queens similar to n queens backtracking problem
leetcode direct

21)String reorder:
also one check -> maxfreq>(n+1)/2 then not possible

Max Heap
Take biggest
Hold previous
Reinsert previous

dry run would be for AAABB -> A=3 and B=2

maxheap take A res=A
	don'tpush A yet B(2) is present in heap prev=A(2)
	then take B res=AB now push A back A(2) B(1) prev=B(1)
then take A res=ABA now push B(1)
 A(1) B(1) then 
B again prev=B(0)
last A(1) return to res-> ABABA

again the code is big so pairs-> ch and freq -> offer into maxheap then sort by freq -> while(!pq.isEmpty())
{
Pair p=q.poll(); ans.append(p.ch) p.frwq--;
if(prev!=null && prev.freq>0) pq.offer(prev.ch,prev.freq)
prev=curr
}
return ans;

22)grid path description-> similar to knight moves grid except check for valid path if we encounter ? that needs to be dealt if path[idx]=='?' then d=0;d<4;d++ newrow newcol based on d as dirs[d] dirs[d] then check for valid -> ans+=dfs(newrow,newCol,steps+1) return ans also proper pruning needs to be done so [important question]


